/**
 * Represents time units for defining or adjusting cookie expiration.
 * Useful for TTL logic and duration-based operations.
 */
export enum TimeUnit {
    Seconds = "seconds",
    Minutes = "minutes",
    Hours = "hours",
    Days = "days",
    Permanent = "permanent",
}
