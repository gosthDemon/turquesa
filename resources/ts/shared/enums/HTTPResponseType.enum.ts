/**
 * Enum representing the different types of HTTP responses.
 * It categorizes the response based on its significance.
 */
export enum ResponseType {
    INFO = 1,
    OK = 2,
    WARNING = 3,
    ERROR = 4,
}
